package sh.damon.remoteunlock.proximity

import android.content.Context

object NotificationHelper {
    fun postConfirm(ctx: Context, mac: String) {
        // Filled in Task 11 once Notifee actions wired
    }
}
